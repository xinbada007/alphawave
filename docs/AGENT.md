# AlphaWave 协同开发文档 (AGENT.md)

## 项目概述
AlphaWave 是一个金融数据分析框架，支持多用户协同使用，每个用户可以配置自己的API密钥进行独立的数据分析。

## 协同开发理念
- **透明协作**: 所有代码变更和设计决策都需要清晰记录
- **安全优先**: 用户敏感信息（API密钥）必须加密存储
- **用户隔离**: 每个用户的配置和数据相互独立
- **易用性**: 提供简单直观的用户配置工具

## 项目架构

### 核心组件
1. **数据收集器 (Collectors)**: 从不同提供商获取金融数据
2. **处理器 (Processors)**: 计算技术指标和其他分析
3. **可视化器 (Visualizers)**: 生成图表和报告

### 安全配置系统
- **加密存储**: 使用cryptography库加密用户API密钥
- **用户隔离**: 每个用户拥有独立的配置文件
- **环境变量**: 支持通过环境变量覆盖配置

## 用户配置流程

### 新用户设置
1. 运行配置脚本: `python3 configure_user.py --user-id <your_user_id>`
2. 按提示输入API密钥
3. 验证配置: `python3 configure_user.py --user-id <your_user_id> --action show`

### 支持的API提供商
- Polygon (通过Massive API)
- Financial Modeling Prep (FMP)
- Alpha Vantage
- Tiingo
- Yahoo Finance (yfinance)

## 代码规范

### Python标准
- 使用Python 3.8+
- 遵循PEP 8编码规范
- 类型注解必需
- 文档字符串必需

### 安全要求
- 敏感信息不得硬编码
- 所有用户数据必须加密存储
- 避免在日志中输出敏感信息

## 配置管理

### 用户配置文件
- 存储位置: `user_configs/<user_id>.json`
- 格式: 加密JSON
- 内容: API密钥、默认设置等

### 环境变量
- `CONFIG_PASSWORD`: 用于加密/解密配置的密码
- 各类API密钥: `POLYGON_API_KEY`, `FMP_API_KEY` 等

## 开发工作流

### 功能开发
1. Fork项目
2. 创建功能分支
3. 实现功能并编写测试
4. 提交PR前确保安全检查

### 安全审查
- 所有涉及敏感数据的变更必须经过安全审查
- 避免引入新的安全漏洞
- 定期更新依赖库

## 项目依赖

### 核心依赖
- openbb: 金融数据获取
- pandas: 数据处理
- numpy: 数值计算
- requests: HTTP请求

### 安全依赖
- cryptography: API密钥加密存储

### 可视化依赖
- quickchart: 图表生成

## 维护指南

### 用户支持
- 新用户加入时，引导其使用配置工具
- 定期提醒用户更新API密钥
- 提供故障排除文档

### 安全维护
- 定期检查加密算法的安全性
- 监控依赖库的安全更新
- 验证所有输入数据的安全性

## 扩展方向

### 功能扩展
- 支持更多数据提供商
- 增加更多技术指标
- 提供更丰富的可视化选项

### 安全增强
- 多因素认证
- 更强的加密算法
- 密钥轮换机制

## 团队分工

### 角色分配
- **核心负责人** (1人): 项目整体规划、架构设计、代码审查
- **数据获取组** (2人): 负责股票数据获取（项目重点）
- **功能开发组** (1人): 技术指标计算和可视化
- **安全运维组** (1人): 系统安全和运维保障

### 关键绩效指标
- 数据获取成功率 > 99%
- API响应时间 < 2秒
- 数据准确性 > 99.5%
- 系统可用性 > 99.5%
- API密钥零泄露事件

---

*此文档持续更新，反映项目发展和最佳实践*